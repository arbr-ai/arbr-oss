package {{packageDomain}}.types

import com.arbr.platform.object_graph.common.model.view.ResourceKeyResolver
import {{packageDomain}}.types.naming.NamedProperty
import {{packageDomain}}.types.naming.NamedResource
import {{packageDomain}}.types.naming.NamedResourceKey

{{#schema}}
object {{titleName}}ResourceKeyResolver: ResourceKeyResolver<{{titleName}}ResourceKey> {
    override fun resolveKey(resourceKey: {{titleName}}ResourceKey): NamedResource<*, {{titleName}}ResourceKey, *, *> {
        return when (resourceKey) {
            {{#table}}
            {{pSchemaTitleName}}ResourceKey.{{pSchemaConstantName}}__{{pTableConstantName}} -> {{pSchemaTitleName}}.{{titleName}}
            {{/table}}
        }
    }
}
{{/schema}}
